<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.winter.app.users.UsersDAO">
    	<insert id="register" parameterType="UsersDTO">
    		INSERT INTO users (username, password, name, email, phone, birth)
    		VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, #{birth})
    	</insert>
    	
    	<delete id="delete" parameterType="UsersDTO">
    		DELETE FROM users WHERE username=#{username}
    	</delete>
    	
    	<select id="detail" parameterType="String" resultType="UsersDTO">
		    SELECT 
		        username,
		        password,
		        name,
		        email,
		        phone,
		        birth
		    FROM users
		    WHERE username = #{username}
		</select>
		
		<update id="update" parameterType="UsersDTO">
			UPDATE users 
			<set>
				name=#{name},
				email=#{email}
			</set>
		</update>
    </mapper>